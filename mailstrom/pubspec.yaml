name: mailstrom
description: 'Gmail inbox cleanup tool — analyze by sender, bulk delete, unsubscribe.'
publish_to: 'none'
version: 1.2.1+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1
  drift: ^2.22.1
  sqlite3_flutter_libs: ^0.5.28
  path_provider: ^2.1.5

  googleapis: ^13.2.0
  googleapis_auth: ^1.6.0
  url_launcher: ^6.3.1
  timeago: ^3.7.0
  http: ^1.3.0
  flutter_widget_from_html_core: ^0.15.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.22.1
  build_runner: ^2.4.14
  riverpod_generator: ^2.6.3
  custom_lint:
  riverpod_lint:
  flutter_launcher_icons: ^0.14.3

flutter_launcher_icons:
  android: false
  ios: false
  image_path: 'assets/app_icon.png'
  macos:
    generate: true
    image_path: 'assets/app_icon.png'

flutter:
  uses-material-design: true
  assets:
    - assets/google_oauth_credentials.json
